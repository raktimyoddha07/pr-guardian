# --- Web framework ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0

# --- DB (async) ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.1            # needs cp314 wheel → use a recent release
alembic>=1.14.0
pgvector>=0.3.6

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.1            # pin so passlib's bcrypt backend keeps working
email-validator>=2.2.0   # for Pydantic EmailStr

# --- HTTP client (GitHub API) ---
httpx>=0.28.1
cryptography>=44.0.0     # GitHub App JWT signing

# --- Config ---
pydantic>=2.11
pydantic-settings>=2.9

# --- Orchestration (LangGraph pipeline) ---
langgraph>=0.2.60
